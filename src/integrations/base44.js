export const functions = {
    invoke: async (name, params) => {
        console.log(`base44.functions.invoke stub: ${name}`, params);
        return { data: null };
    }
};
export const entities = {};
export const integrations = {};
